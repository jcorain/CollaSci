'''
module where the different tables are created 
'''

def create data_table(path = os.path.dirname(os.getcwd()), dbname = 'database.sqlite'):
    """
    

    Parameters
    ----------
    path : str, optional
        DESCRIPTION. The default is os.path.dirname(os.getcwd()).
    dbname : TYPE, optional
        DESCRIPTION. The default is 'database.sqlite'.

    Returns
    -------
    None.

    """
